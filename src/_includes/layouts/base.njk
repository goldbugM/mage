<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }} | Premium Chauffeur</title>
    <meta name="description" content="{{ description or 'Discover luxury travel experiences in Germany and Europe with Premium Chauffeur.' }}">
    <meta name="keywords" content="{{ keywords or 'luxury travel, premium chauffeur, European destinations' }}">
    
    <!-- Open Graph -->
    <meta property="og:title" content="{{ title }} | Premium Chauffeur">
    <meta property="og:description" content="{{ description or 'Explore Germany\'s finest destinations and European luxury experiences.' }}">
    <meta property="og:type" content="website">
    <meta property="og:image" content="{{ heroImage or '/public/images/default-hero.jpg' }}">
    
    <!-- Canonical URL -->
    <link rel="canonical" href="https://premium-chauffeur.com{{ page.url }}">
    
    <!-- Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/public/css/main.css">
    
    {% block head %}{% endblock %}
</head>
<body>
    {% include "partials/header.njk" %}
    
    <main>
        {% block content %}
        {{ content | safe }}
        {% endblock %}
    </main>
    
    {% include "partials/footer.njk" %}
    
    <!-- JavaScript -->
    <script src="/public/js/LightRays.js"></script>
    <script src="/public/js/main.js"></script>
    {% block scripts %}{% endblock %}
</body>
</html>
